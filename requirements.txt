# bittensor 8.5.x pins fastapi~=0.110; use 9.x for current finney runtime decode compatibility
fastapi==0.110.3
uvicorn[standard]==0.30.6
jinja2==3.1.4
python-multipart==0.0.12
apscheduler==3.10.4
bittensor==9.12.2
# Pin to 1.x: bittensor 9.12.2 imports ScaleObj which 2.x dropped, and 2.x
# also pulls cyscale which collides with scalecodec namespace. 1.5.6+ is the
# floor that bittensor accepts.
async-substrate-interface>=1.5.6,<2.0.0
requests==2.32.3
certifi>=2024.0.0
python-dateutil>=2.8.0
python-dotenv>=1.0.0
# Daily digest LLM composer (Claude Haiku via Anthropic API)
anthropic>=0.40.0
# Emission-lab property tests (§4.6)
pytest>=8.0.0
